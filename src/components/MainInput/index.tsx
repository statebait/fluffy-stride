import React from "react"
import "./style.scss"

const MainInput = () => {
  return (
    <input
      id="encrypt-input"
      type="email"
      name="email"
      placeholder="Enter your sentence here!"
    />
  )
}

export default MainInput
